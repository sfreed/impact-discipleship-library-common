// The VALUES are what sit in Firestore's `testimonials.type`, so they are
// the stable part - rename a member freely, never a value.
export enum TESTIMONIAL_TYPES     {
    COACHING = 'Coaching with Impact',
    EQUIPPING_GROUPS = 'Equipping Groups',
    HOME = 'Home',
    SEMINARS = 'Seminars'
}
